/**
 * AI Page - Main entry point for the AI assistant feature.
 * Routes to the chat shell interface.
 */

import { ChatShell } from "./components/chat-shell";

const AiPage = () => {
  return <ChatShell />;
};

export default AiPage;
